# SSRM guidance

Keep the incoming request schema close to ag-Grid SSRM. Add FinA extensions in namespaced fields. Compile only validated column, filter, sort, grouping, and aggregation expressions; never interpolate untrusted identifiers or values directly into SQL.
