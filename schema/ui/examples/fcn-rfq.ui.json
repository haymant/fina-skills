{
  "registry": "fina/ui",
  "version": 1,
  "kind": "page",
  "id": "trading.rfq.fcn",
  "title": "FCN RFQ",
  "description": "Schema-driven FCN RFQ capture and quote workflow. Compose a structured request, submit it via the rfq.create logical operation, then price on the Quotes screen.",
  "schema": "trade.rfq-create",
  "layout": "trading.rfq.fcn-layout",
  "workflow": "fcn.rfq",
  "edit_modes": [
    "editable",
    "read-only",
    "diagnostic"
  ],
  "children": [
    {
      "kind": "workflow",
      "workflow": "fcn.rfq"
    },
    {
      "kind": "text",
      "text": "Submit invokes the rfq.create logical operation. A price result is never auto-persisted as a quote."
    }
  ]
}
